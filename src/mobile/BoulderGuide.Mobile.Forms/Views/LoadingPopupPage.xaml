<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
             x:Class="BoulderGuide.Mobile.Forms.Views.LoadingPopupPage">

   <pages:PopupPage.Animation>
      <animations:ScaleAnimation PositionOut="Bottom"/>
   </pages:PopupPage.Animation>

   <pages:PopupPage.Content>
      <Grid>
         <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90*"/>
         </Grid.ColumnDefinitions>
         <Frame CornerRadius="5" Padding="5" VerticalOptions="Center"
                   HeightRequest="200" BackgroundColor="#0000" HasShadow="True" Grid.Column="0" Grid.Row="0" Margin="10,0,10,0">
            <ActivityIndicator
               Color="{StaticResource Key=Accent}"
               Scale=".5"
               Grid.Column="0"
               Grid.Row="0"
               IsVisible="True"
               IsRunning="True"
               IsEnabled="True"
               x:Name="BusyAI"/>
         </Frame>
      </Grid>
   </pages:PopupPage.Content>
</pages:PopupPage>